<h1>ScdReglaSms creation</h1>

<form action="{{ path('admin_regla_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_regla') }}">
            Back to the list
        </a>
    </li>
</ul>
