<h1>ScdDenuncia</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Mensajedenuncia</th>
            <td>{{ entity.mensajedenuncia }}</td>
        </tr>
        <tr>
            <th>Fechahorainicio</th>
            <td>{{ entity.fechahorainicio|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fechahorafin</th>
            <td>{{ entity.fechahorafin|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Denunciaactiva</th>
            <td>{{ entity.denunciaactiva }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('admin_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
